---
title: Roadmap
description: Where the DriftQ project is going next (v2 and beyond).
---

<div className="space-y-10">
  <section className="space-y-4">
    <div className="rounded-2xl border border-white/10 bg-white/[0.03] px-4 py-3 text-white/90">
      <p className="m-0">
        <strong>Note:</strong> this roadmap is directional and subject to change.
      </p>
    </div>

    <div className="rounded-2xl border border-white/10 bg-white/[0.03] px-4 py-3 text-white/90">
      <p className="m-0">
        <strong>DriftQ</strong> is the overall project/org.{" "}
        <strong>DriftQ-Core</strong> is the main GitHub repo that contains the v1 broker implementation.
      </p>
    </div>
  </section>

  {/* v1 */}
  <section className="space-y-5">
    <h2 className="m-0 flex flex-wrap items-center gap-2">
      <span>v1: DriftQ-Core (MVP reliability foundation)</span>
      <span className="inline-flex items-center rounded-full border border-emerald-300/40 bg-emerald-300/15 px-2 py-0.5 text-xs font-medium text-emerald-100">
        Completed
      </span>
    </h2>

    <p className="m-0">
      v1 is the reliability layer: the broker semantics you need before any workflow system can be trusted.
    </p>

    <ul className="space-y-2">
      <li><strong>Leasing</strong> — safe ownership + crash recovery</li>
      <li><strong>Retries</strong> — backoff + max attempts</li>
      <li><strong>DLQ</strong> — strict dead-letter routing</li>
      <li><strong>Idempotency</strong> — prevent duplicate side effects under redelivery</li>
      <li><strong>Observability</strong> — inflight, lag, DLQ totals, backpressure rejects</li>
    </ul>

    <div className="rounded-2xl border border-white/10 bg-white/[0.03] px-4 py-3 text-white/90">
      <p className="m-0">
        <strong>Status:</strong> v1 MVP is <em>complete</em>. Next is building “workflow-level” capability on top of it.
      </p>
    </div>
  </section>

  {/* v2 */}
  <section className="space-y-5">
    <h2 className="m-0 flex flex-wrap items-center gap-2">
      <span>v2: Replayable Workflow Runtime — Time-travel debugging and safe retries for AI workflows</span>
      <span className="inline-flex items-center rounded-full border border-emerald-300/40 bg-emerald-300/15 px-2 py-0.5 text-xs font-medium text-emerald-100">
        Completed
      </span>
    </h2>

    <p className="m-0">
      v2 turns the reliability foundation into a practical workflow engine focused on AI pipelines and RAG.
    </p>

    <ul className="space-y-2">
      <li><strong>Packaging</strong> &amp; docs — Docker local run path + v2 foundations docs</li>
      <li><strong>Replayable runs</strong> — durable run state + append-only event log; deterministic DAG scheduling; time-travel replay + live re-drive from step N; artifacts + replay cache</li>
      <li><strong>Workflow primitives</strong> — durable timers/delay (survive restarts), cancel propagation, join/barrier via DAG deps, per-node timeouts</li>
      <li><strong>Budgets &amp; throttles</strong> — max attempts + wall-clock timeout; optional token/$ usage tracking (best-effort); concurrency caps (topic + tenant/topic)</li>
      <li><strong>Developer tooling</strong> — driftqctl runs list/inspect/timeline/diff/replay + promote/rollback; topics lag/peek helpers</li>
      <li><strong>Observability</strong> — request_id + trace_id correlation; step timing metrics; /debug/metrics snapshot</li>
      <li><strong>Templates &amp; demos</strong> — built-in demo handlers + /debug/run-demo + /debug/run-spec to start runs fast</li>
    </ul>

    <div className="rounded-2xl border border-white/10 bg-white/[0.03] px-4 py-3 text-white/90">
      <p className="m-0">
        <strong>Intent:</strong> make AI workflows reproducible, debuggable, and safe to retry — not "best-effort scripts."
      </p>
    </div>
  </section>

  {/* Future */}
  <section className="space-y-5">
    <h2 className="m-0">Future (planned / TBD)</h2>

    <ul className="space-y-3">
      <li className="flex flex-wrap items-center gap-2">
        <span className="font-semibold">v3: Multi-Agent Runtime &amp; Real-Time AI</span>
        <span className="inline-flex items-center rounded-full border border-white/20 bg-white/10 px-2 py-0.5 text-xs font-medium text-white/80">
          Planned
        </span>
      </li>

      <li className="flex flex-wrap items-center gap-2">
        <span className="font-semibold">v4: DriftQ Cloud (Managed AI Orchestration Platform)</span>
        <span className="inline-flex items-center rounded-full border border-white/20 bg-white/10 px-2 py-0.5 text-xs font-medium text-white/80">
          Planned
        </span>
      </li>

      <li className="flex flex-wrap items-center gap-2">
        <span className="font-semibold">v5: AI Nervous System (Long-Term Vision)</span>
        <span className="inline-flex items-center rounded-full border border-white/20 bg-white/10 px-2 py-0.5 text-xs font-medium text-white/80">
          Planned
        </span>
      </li>
    </ul>

    <div className="rounded-2xl border border-white/10 bg-white/[0.03] px-4 py-3 text-white/90">
      <p className="m-0">
        <strong>Expectation:</strong> details for future versions stay intentionally high-level until they’re actively built.
      </p>
    </div>
  </section>

</div>
